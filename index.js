const name = "Susan";// changed the name variable t string susan
const height = 39; // use a number that is less than 40 and greater than 0
const message = `${name} is ${height} inches tall`;

module.exports = {name, height, message};
